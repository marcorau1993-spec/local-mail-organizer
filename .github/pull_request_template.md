## Summary

Describe the problem and the solution.

## Verification

- [ ] Python tests pass (`python -m pytest -q`)
- [ ] Python lint passes (`python -m ruff check src tests`)
- [ ] Frontend lint passes (`npm run lint`)
- [ ] Frontend production build passes (`npm run build`)

## Safety and privacy

- [ ] No credentials, tokens, mailbox addresses, subjects, bodies, attachments, logs, databases, exports, or personal paths are included.
- [ ] Mailbox mutations still require explicit user approval.
- [ ] Provider-specific behavior has a safe failure mode.
- [ ] New AI behavior is advisory, explainable, and bounded by deterministic protection rules.

## Screenshots

Use synthetic data only. Remove this section when no interface change is involved.
